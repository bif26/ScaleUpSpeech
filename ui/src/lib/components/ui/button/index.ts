import Root from './Button.svelte';
import { type ButtonVariant, type ButtonSize, buttonVariants } from './button-variants';

export {
  Root,
  Root as Button,
  buttonVariants,
  type ButtonVariant,
  type ButtonSize,
};
